import { expect } from "chai";

describe("Game Functionality", () => {
    it("should return the number of guesses left", () => {
        const arrayCheck: string[] = ["hi", "there"];
        expect(arrayCheck.length).to.equal(2);
    });
});
